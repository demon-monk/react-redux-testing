import React from 'react'

export default ({commentList}) => {
    return (
        // commentList ? <ul>
        //     {
        //         commentList.map(comment => <li>{comment}</li>)
        //     }
        // </ul> : null
        <div>CommentList</div>
    )
}